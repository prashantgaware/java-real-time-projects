input {
  tcp {
    port => 5000
    codec => json
  }
}

filter {
  if [logger_name] {
    mutate {
      add_field => { "service" => "%{[logger_name]}" }
    }
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "hunger-saviour-logs-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}
